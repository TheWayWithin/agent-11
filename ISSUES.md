# agent-11 — Issue & Project Register

**This is the single source of truth for what is open in this repo.** One row per
issue/project. Detail lives in the linked doc; this file is the index the Mission
Control reconcile (`repo-reconcile.py`) reads and mirrors to the cockpit.

## ID convention (collision-safe)

Mission Control owns the bare `ISS-`/`PRJ-`/`T-` namespaces. **Every agent-11 ID
carries the `A11-` prefix** so it can never collide with a Mission-Control-native
ID or another repo's. Raise issues here with `python3 ~/shared/scripts/repo-issue.py`.

---

## Open

| ID | Title | Status | Severity | Detail | MC-SYNC |
|----|-------|--------|----------|--------|---------|
| A11-ISS-30 | MCP config ships unscoped credentials: Supabase full-access token, filesystem rooted at whole DevProjects, account-wide Figma token, alongside unrestricted Playwright web write (exfiltration trifecta reported by external researcher) | Open | high | — | pending |
| A11-ISS-29 | /blog and /dailyreport diverge from the canonical content standard on seven points, and one of them prints an irreversible command by default. Reviewed 2026-08-14 against ~/shared/reference/content-standard.md and the four user skills. THE URGENT ONE: /blog Step 10 and dailyreport.md:534 both print 'jpub <path> --all'. The content standard forbids exactly this under ISS-45 — give the blog command ONLY, never the social command in the same message — because on 2026-08-03 both were printed together with the words 'review everything before it ships' and the social went out unreviewed in the same breath. The asymmetry is the reason: a blog post is recoverable since jpub upserts by slug, while a tweet, LinkedIn post and WIP todo are seen, cached and indexed the moment they land, and LinkedIn cannot be read back to confirm. On 2026-08-13 the command told Jamie to run --all and he happened to run --blog; that is luck, not design. The same rule already lives in skills/user/jamie-publish/SKILL.md and neither command was updated. SIX MORE, all verified rather than inferred: (1) titles are invented locally though jamie-titles' own frontmatter says the content skills MUST call it — the title /blog produced on 08-13 scored 3/5 on that skill's rubric, below its own discard threshold; (2) Output 5 image specs are missing entirely, so packages ship with no image and jpub then posts text-only social silently — the 08-13 post had no image until Jamie asked, and the filename was written as -image-spec.md against the standard's -images.md; (3) staging goes to a repo-local blog/ rather than ~/shared/content/drafts/, which is not just non-standard but unreachable, since DevProjects does not sync between machines and a Mini-scheduled post cannot see it — a third location, ~/shared/content/blog/, had to be invented by hand on 08-13 to schedule the socials; (4) frontmatter omits topics and editorialType, the two fields the site's taxonomy actually indexes on, so jpub warned 'Topics: none — will not appear on topic pages' and the post would have been findable only by direct link; (5) the X output is a single tweet where the standard specifies a 6-10 tweet thread, and LinkedIn diverges on length, closing line and hashtag count; (6) the WIP hashtag default is inverted, deriving a product tag from the repo name when the standard defaults to #jamiewatters and uses a product tag only when the post genuinely is about that product, because on WIP the tag files the todo and a wrong one misfiles it; (7) the voice guide resolves to a bundled copy at .claude/data/voice-guide-default.md rather than the jamie-voice skill that calls itself the VOICE AUTHORITY — a second source of truth, and jamie-titles already carries a header warning that its own duplicate 'is a paste and drifts silently'. Full review with fixes and a suggested order: insights/blog-dailyreport-vs-content-standard-2026-08.md. Items 1-4 of that order are small and each closes a proven failure | ✅ Resolved 2026-08-14 — All seven divergences fixed in project/commands/{blog,dailyreport}.md, with .claude/commands copies kept identical (the dailyreport pair had already drifted 30 lines apart and is now byte-identical). THE URGENT ONE: the all-platforms flag no longer appears in either file at all; the report prints the --blog command alone and stops, and the social command sits in its own step marked SECOND MESSAGE ONLY. The six others: titles delegated to jamie-titles rather than a second local rubric; Output 5 image specs (-images.md) added, with the palette block required on every route and svg-to-png.sh named; the package now stages to ~/shared/content/drafts/ (repo-local fallback retained for installs with no ~/shared/, and it must now report that a hero cannot upload from there because jpub's image directory is hardcoded); frontmatter carries topics (the controlled seven) and editorialType, plus imageCaption and draft, and drops date/project/author/coverImage; X output is a 6-10 tweet thread and LinkedIn 200-400 words with three hashtags; the WIP hashtag never derives from the repo name; and the voice chain resolves to the canonical site-repo guide, then its synced mirror, with the bundled AGENT-11 copy demoted to a fallback. FOUND WHILE FIXING, not in the review: the agent-11 social file format made the thread impossible. jpub reads a file starting with a '# Twitter/X' heading as 'everything between the first and second ---', which silently truncates a thread to tweet one because those are the same separators. Proven by dry run: identical three-tweet content reports '1 tweet' with the heading and '3 tweets' without. Social files are now plain text. PROOF: scripts/validate-content-commands.sh reported 110 failures across the four files before the edits and exits 0 after; scripts/test-validate-content-commands.py re-introduces each divergence into a throwaway copy and all 19 mutations are caught. A conformed package was dry-run through jpub: Topics now reads 'building, open-source' where the 08-13 post reported '(none - will not appear on topic pages)'. Two critics on different models, given only the standard, the four skills and the two files, then found ten further gaps - the post URL was conditional in all three social outputs, dailyreport staged one public WIP todo per milestone, WIP lines carried no URL, --wip with no file makes jpub invent one - all fixed and now covered by the validator. Commits 3748622, 00aa2f4, 5638cbf. NOT DONE: the 40 deployed copies across the fleet still carry the old text and need an install.sh --upgrade sweep. | high | — | pending |
| A11-ISS-28 | fleet-shipped radar overstates unshipped work: 'unpushed' measures against the tracking branch only, and intentionally parallel branches report as unmerged | ✅ Resolved 2026-08-06 — UNPUSHED now measured as 'git rev-list --count HEAD --not --remotes' (reachable from HEAD, reachable from no remote-tracking ref) instead of '<upstream>..HEAD', so commits already published on any remote ref stop counting as laptop-only. Added a registry-driven 'parallel_branches' key and set it to main on PlebTest and modeloptix, whose main is the production landing page promoting the app while it is built on develop: divergent by design, never merged back, so it is no longer reported as an unmerged branch. Verified with a throwaway repo and a real remote: all-pushed reads 0, one and two genuine laptop-only commits still read 1 and 2 (so the column is not merely always-zero), and the exact false-alarm shape (stale feature-branch upstream, work already on origin/main) reads 1 under the old measurement and 0 under the new. Fleet run went from 9 repos 'with something outstanding' to 5, with agent-11-website, PlebTest and modeloptix all clearing correctly and no repo losing a genuine finding. | medium | Two false-alarm defects found 2026-08-06 while clearing the fleet. (1) UNPUSHED is computed as rev-list origin/<tracking-branch>..HEAD, so commits already published on another remote ref count as laptop-only. agent-11-website reported '4 commits exist only on this machine' while HEAD, main and origin/main were all at f493185 and every one of those commits was on origin/main; the only lagging ref was the stale feature branch origin/fix/a11w-iss-19-overflow. The honest question is 'reachable from HEAD and from no remote at all', i.e. rev-list HEAD --not --remotes. (2) PlebTest and modeloptix deliberately keep main as a production landing page promoting the app while it is built on develop; main will never merge into develop, so the permanent divergence is reported as an UNMERGED BRANCH forever. A report that always contains known noise stops being read. | pending |
| A11-ISS-27 | fix-fleet-permissions.py parses tier but never reads it, so --apply writes settings.json into every fleet repo including different-framework and archived tiers | ✅ Resolved 2026-08-05 — Duplicate of A11-ISS-25, which fixed this the same day (the note originally cited A11-ISS-26, which is the unrelated fleet-shipped denylist fix; corrected 2026-08-06). Verified on disk at scripts/fix-fleet-permissions.py:152-160: a tier ALLOWLIST now gates the sweep (DEPLOYED_TIERS), matching bulk/lib/parse-registry.py, with an explicit --repo deliberately overriding it. Raised in error from a stale survey; no code change needed. | high | — | pending |
| A11-ISS-26 | fleet-shipped.sh tier denylist omits archived, so the retired digital-estate repo reports as unshipped work forever. scripts/fleet-shipped.sh:110,113 skips only ('skip','different-framework'); tier archived was documented in registry.yaml on 2026-08-05 (ISS-57) and digital-estate still has .git and .claude on disk at DevProjects/Archive/digital-estate, so it will show as UNCOMMITTED/UNPUSHED noise in every report. Read-only script, so noise not danger. Fix: add 'archived' to the denylist tuple. | ✅ Resolved 2026-08-05 — fleet-shipped.sh now excludes archived via a named EXCLUDED_TIERS tuple. Verified: digital-estate no longer appears in the report. | low | — | pending |
| A11-ISS-25 | fix-fleet-permissions.py parses tier: but never filters on it, so --apply rewrites .claude/settings.json in every registry entry including tier archived, skip and different-framework. Found 2026-08-05 while auditing all 20 registry consumers for ISS-57. Every other writing consumer (apply-file.sh, apply-upgrade.sh, via bulk/lib/parse-registry.py) gates on a tier ALLOWLIST; this one reads the field at scripts/fix-fleet-permissions.py:91 and then ignores it at :145-150. thewaywithin is tier different-framework and the registry header says bulk ops must never touch it. Fix: add the same allowlist the other writers use. | ✅ Resolved 2026-08-05 — fix-fleet-permissions.py now gates the sweep on DEPLOYED_TIERS (active/local-only/dormant), the same allowlist validate-fleet-permissions.sh uses; an explicit --repo still overrides. Dry run confirms 6 entries now out of scope (3 skip, archived, sandbox, different-framework) where before it would have written to all of them. Separately fixed the summary line, which was counting tier-skips under 'unparseable'. | high | — | pending |
| A11-ISS-24 | install.sh dry-run plan guessed the settings.json merge outcome by grepping two of merge-settings.py's three no-op conditions, reporting no-op whenever permissions were what differed | ✅ Resolved 2026-08-05 — the dry-run plan now runs merge-settings.py against a temp copy and reports what it actually returns, instead of inferring the outcome from two of its three conditions | high | — | pending |
| A11-ISS-23 | install.sh auto-executes mcp-setup.sh whenever .env.mcp exists, with no prompt and no dry-run visibility: it runs 'npm install -g' for 7 packages (writes outside the project entirely), 'claude mcp remove <name> -s project' for 10 hardcoded servers (wipes existing project MCP registrations), and writes .mcp-status.md at the project root. install.sh also overwrites the repo's mcp-setup.sh with a freshly downloaded copy first. Of T-245's seven in-scope repos only Trader-7 has .env.mcp - the live-on-Railway one. The dry run cannot show any of this because it short-circuits before setup_mcp_configuration. Separately, .mcp.json is downloaded unconditionally with no existence check (only 404s today because it is gitignored upstream), so a customised .mcp.json would be clobbered with no backup if that ever resolves | ✅ Resolved 2026-08-05 — mcp-setup.sh execution is now behind --with-mcp, defaulting off, so an install no longer runs global npm installs or tears down project MCP registrations because .env.mcp happens to exist; the dry-run plan reports both branches; .mcp.json is no longer written over an existing file; scripts/validate-mcp-optin.sh asserts all three structurally and was negative-tested | high | — | pending |
| A11-ISS-22 | library/settings.json.template ships a prompt-type PreToolUse hook whose if-glob fails open on multi-line/heredoc/redirection commands, so the destructive-command prompt fires on benign Bash and its judge silently refuses it in every deployed repo | ✅ Resolved 2026-08-05 — library/hooks/destructive-guard.sh ships and is wired as a command-type PreToolUse hook, replacing the prompt-type if-glob that failed open on multi-line commands; scripts/validate-destructive-guard.sh green. Deployed to all 17 active repos by the T-245 sweep | high | — | pending |
| A11-ISS-21 | install.sh treats a bare handoff-notes.md as a v5 marker, so 'install.sh --upgrade' on an already-v6 repo invokes migrate-v5-to-v6.sh, which DELETES handoff-notes.md and folds it into agent-context.md. Confirmed by dry run on JamieWatters. Would have hit 2 of T-245's 7 in-scope repos including Trader-7, whose handoff-notes.md is a 426-line TRACKED project file in a repo running live on Railway. Neither repo has any structural v5 marker (.mcp-profiles/, mcp/dynamic-mcp.json, templates/handoff-notes-template.md) - v6 itself uses handoff-notes.md, so it is not a v5 signal on its own. agent-11's own repo trips the same detector. Also: migrate-v5-to-v6.sh writes its backup INSIDE the repo (.claude/backups/v5-to-v6-*) | ✅ Resolved 2026-08-05 — v5 detection now requires one of the three STRUCTURAL markers (.mcp-profiles/, mcp/dynamic-mcp.json, templates/handoff-notes-template.md); a bare handoff-notes.md is reported as an advisory and triggers nothing. Verified all seven in-scope repos report 'No v5.x markers' - it would have deleted Trader-7's 426-line tracked handoff file | high | — | pending |
| A11-ISS-20 | install.sh.sha256 is stale since commit a18ddc9 (2026-08-03), so secure-install.sh — the ONLY install command in the README — hard-fails with 'Checksum verification FAILED. The installer may have been tampered with.' and exits 1. Anyone installing agent-11 from main today cannot. Root cause: three commits edited install.sh without regenerating the checksum and nothing compared the two | ✅ Resolved 2026-08-05 — install.sh.sha256 regenerated; scripts/validate-installer-checksum.sh added and negative-tested three ways (edited installer, misnamed target, deleted file) so secure-install.sh can never again refuse every install because nothing compared the two | high | — | pending |
| A11-ISS-19 | registry.yaml marks executor-file and executor-file-site tier: active but neither has any AGENT-11 deployment (no .claude/, no missions/); they inflate every fleet metric and make fleet audits report false drift - decide deploy vs reclassify tier (evidence in Ideation/fleet-audit-2026-08-04.md) | ✅ Resolved 2026-08-04 — registry.yaml: executor-file and executor-file-site moved from tier active to tier skip, with notes recording that agent-11 is not installed (no .claude/ at all) and that deploying it is an open PRODUCT decision for Jamie, deliberately not taken here. Neither repo was touched. validate-fleet-permissions.sh now fails on any entry whose tier claims deployment where there is no .claude/agents/coordinator.md, so this cannot silently return. Reported but NOT changed: ASMGE (tier active), mcp-7 and mcp-11 (dormant) have no path on this machine — DevProjects does not sync between Jamie's machines, so absence here is not evidence of absence | medium | — | pending |
| A11-ISS-18 | Ten deployed repos set defaultMode: bypassPermissions in .claude/settings.local.json, which defeats the read-only gate deny rules; settings.local.json is user-owned so merge-settings.py does not manage it and the T-245 v6.2.0 sweep will not fix it (SEOAgent, aisearchmastery, freecalchub, BOS-AI, aimpactscanner-mvp, llm-txt-mastery, JamieWatters, solomarket, evolve-7, agent-11-website; evidence in Ideation/fleet-audit-2026-08-04.md) | ✅ Resolved 2026-08-04 — TWELVE repos, not the ten recorded — the audit's top-3-per-repo cap hid test-project and agent-11 ITSELF. All twelve .claude/settings.local.json moved from defaultMode bypassPermissions to acceptEdits with blanket bare Bash/Edit/Write allows removed; env, deny, ask, key order and every specific/MCP grant preserved (verified 12/12 against backups). MECHANISM IN THE ISSUE WAS WRONG: deny rules apply in every mode and deny beats allow, so neither setting made the deny rules inert. The real defeat is that .claude is a protected path whose writes are unprompted under bypassPermissions, so an agent could delete the deny rules then edit the gate; acceptEdits restores that prompt. New scripts/validate-fleet-permissions.sh demonstrated failing before (exit 1, 50 breaches) and passing after (exit 0); also checks user scope, since permission rules merge across scopes. Cold review found the blanket detector missed Edit(//**)-style anchor forms — fixed and unit-tested. NOTE: the four deny rules are still absent fleet-wide; that is T-245's sweep, not this | high | — | pending |
| A11-ISS-17 | Five deployed missions have no invocation route: /coord's routing table covers 13 of 18, so connect-mcp, architecture, product-description, operation-genesis and operation-recon hard-error on /coord and are unreachable | ✅ Resolved 2026-08-04 — Routing table now carries an explicit File column and all 18 executable missions; validate-deployment-coverage.sh compares library, install.sh mission_files, verify_installation() and the routing table plus the unknown-mission help text, negative-tested against six subversions | high | — | pending |
| A11-ISS-16 | gate-guard.sh cannot catch interpreter-mediated writes or variable indirection to gate paths (python3 -c open(...,'w'), G=path; echo > $G); it is a speed bump, not a boundary, and docs must not claim it closes the Bash route | ✅ Resolved 2026-08-04 — Guard hardened 8 to 13 detection branches; two cold reviews on a different model found 9 then 15 live bypasses (dir-prefixed paths, absolute/quoted commands, backslash-newline continuation, brace expansion, xargs, case, bare gates dir) plus one false positive, all closed; probe array 11 to 38 block and 18 allow forms; three surviving closes-the-Bash-route claims removed and validate-bash-route-claims.sh added | medium | — | pending |
| A11-ISS-15 | coordinator.md:265 claims permissions.deny makes any acceptance-criteria test read-only to every agent; only the four static gate rules exist (fourth instance of the same overclaim, see A11-ISS-11) | ✅ Resolved 2026-08-03 — Fixed in 03a78f4 as part of the same claim class. coordinator.md:265 now states that two of the three named things are enforced and the third is not, and that refusing to delegate an edit to an acceptance-criteria test is the coordinator's job rather than the tool layer's. Coordinator logic unchanged. | high | — | pending |
| A11-ISS-14 | code-review-loop skill should default critic and fixer to different models, per the zero-trust verification rule | ✅ Resolved 2026-08-03 — Fixed in 7ce651a. code-review-loop SKILL.md now specifies critic on a different model to the fixer, with how to do it (/model, CLAUDE_CODE_SUBAGENT_MODEL, opts.model), an honest single-model fallback that logs shared weights and downgrades a clean round to weaker evidence, a unanimity warning, and the note that a deterministic score still outranks any cross-model arrangement because it has no blind spots to share. | low | — | pending |
| A11-ISS-13 | install.sh mission_files list omits connect-mcp.md and operation-recon.md, so two library missions never reach deployed projects | ✅ Resolved 2026-08-03 — Fixed in a18ddc9. connect-mcp.md and operation-recon.md added to install.sh's mission_files array (20 entries, matching project/missions exactly). scripts/validate-deployment-coverage.sh compares the array to the directory and exits non-zero on a mismatch in either direction, so a hand-maintained list cannot silently drift again. Negative-tested both ways. | medium | — | pending |
| A11-ISS-12 | mission-optimize Phase 2 should write and verify permissions.deny rules for the run's declared metric command and editable surface | ✅ Resolved 2026-08-03 — Fixed in 03a78f4. mission-optimize Phase 2 now writes Edit(path) deny rules for the run's declared metric command, benchmark and fixtures, with a worked settings.json example, and proves they bite by attempting one edit and attaching the refusal. Two new default-fail success criteria gate the phase on it. Where the metric is a shell one-liner with no file to protect, the mission requires that to be logged and the run treated as watched throughout, rather than pretending a rule exists. | medium | — | pending |
| A11-ISS-11 | mission-optimize claims permissions.deny enforces the metric command and editable surface; only the four static gate rules exist | ✅ Resolved 2026-08-03 — Fixed in 03a78f4. The sweep found 8 instances, not 1: mission-optimize.md, its input template, quality-gates-guide.md, coordinator.md, library/CLAUDE.md, tester.md, developer.md, project/gates/README.md. Each now names the four shipped Edit() rules and separates what is enforced at the tool layer from what is a rule the agent is asked to follow. Regression check at scripts/validate-enforcement-claims.sh (1031b76) reports all 8 against the pre-fix text and is silent on the corrected library. | high | — | pending |
| A11-ISS-10 | permissions.deny read-only-gate rules (v6.2.0) do not propagate to pre-6.2 repos on install.sh --upgrade - merge-settings.py fills the env and hooks gaps only and leaves permissions user-owned; same gap class as A11-ISS-9 but for permissions; consider folding into the A11-ISS-7 deny-rule cleanup | ✅ Resolved 2026-07-23 — merge-settings.py now manages shipped permissions.deny rules like shipped hooks: template Edit() gate rules added if missing, stale v6.2.0 no-op Write()/MultiEdit() forms removed (provably no-ops per A11-ISS-7), user deny/allow entries preserved. Proven: test-merge-settings.sh 28/28; end-to-end install.sh --upgrade on pre-6.2 fixture lands the 4 Edit deny rules and is idempotent; already-6.2 repo re-upgrade is NOOP with unchanged semantic hash; fixture 05 10/10. | low | — | pending |
| A11-ISS-9 | merge-settings.py no-ops when a deployed repo already has a hooks key, so hook fixes in settings.json.template (e.g. the A11-ISS-4 gate-guard rework) never reach existing repos via install.sh --upgrade - needs a versioned or per-hook merge strategy | ✅ Resolved 2026-07-23 — Shipped hooks now managed via STALE_SHIPPED_HOOKS fingerprint catalogue: stale entries replaced, user-authored/edited hooks preserved byte-identical, statusMessage identity prevents duplicates. Proven: test-merge-settings.sh 19/19 + fixture 05 end-to-end --upgrade (gate guard lands, idempotent). Commit 18f8166. | low | — | pending |
| A11-ISS-8 | No plan-tool.yaml template: templates/ has plan-saas-mvp/saas-full/api.yaml (deployed via install.sh) but the new tool project type (A11-ISS-6) has no counterpart - needs a designed phase/agent-distribution/timeline template for CLI toolkits and libraries | ✅ Resolved 2026-07-23 — templates/plan-tool.yaml authored (4 phases: interface design/scaffold, core implementation, hardening/docs, package/release; gates build/test/lint matching /bootstrap tool type; dev-heavy agent distribution; CLI/library-scoped risks, hints, anti-patterns — sibling-consistent schema and depth). Added to install.sh template_files, sha256 regenerated. Proven: YAML validates (4 phases, gates, distribution=100), fixture install.sh --upgrade deploys it byte-identical alongside the three siblings. | low | — | pending |
| A11-ISS-7 | settings.json.template Write()/MultiEdit() permission deny rules are no-ops — Claude Code only matches Edit(path) rules for file tools (warnings at session start); Edit rules already cover every gate path so no security gap, but the dead rules should be pruned to silence the warnings | ✅ Resolved 2026-07-23 — Deny list rewritten to Edit()-only forms (Edit rules cover all file-editing tools; Write()/MultiEdit() forms are ignored with warnings — confirmed empirically on claude 2.1.218). Fixture proof: Write tool to gates/ and .quality-gates.json BLOCKED under new template, src/ write passes, warnings silenced. Gate coverage unchanged (the four Edit rules were already the only effective ones). | low | — | pending |
| A11-ISS-6 | /bootstrap lite-tier gaps: type enum (saas-mvp/saas-full/api) has no CLI-toolkit fit, prerequisites demand vision.yaml lite projects lack, and it prescribes phase-1-context.yaml even when phase 1 is already complete at bootstrap time — PRJ-14 pilot feedback (digital-estate) | ✅ Resolved 2026-07-23 — Added tool project type (enum+inference+gates), vision.yaml now saas-*-only (tool/api proceed from PRD with warning), phase context targets first incomplete phase not unconditionally phase-1. Proven via lite fixture dry-run old-vs-new. Commit 995eb32. | low | — | pending |
| A11-ISS-5 | /foundations PRD extraction prompt is SaaS-biased for lite-tier projects (tier metrics, BR-XXX counts, pricing checks all N/A for a CLI toolkit; worked but most validation is vestigial) — PRJ-14 lite pilot feedback | ✅ Resolved 2026-07-23 — PRD extraction now source-driven: SCOPE RULE + conditional SaaS-shaped items (BR-XXX, state machines, tier metrics); FAIL conditions scoped. Proven via lite CLI-toolkit PRD dry-run: old FAIL -> new PASS with N/A checks. Commit 21eae2a. | low | — | pending |
| A11-ISS-4 | PreToolUse read-only gate guard false-positives on non-gate Bash (blocked a python-detection for-loop during /foundations init in digital-estate) | ✅ Resolved 2026-07-23 — Guard decision moved to .claude/hooks/gate-guard.sh (inspects real Bash command from hook stdin); fail-open if-glob removed. Proven: for-loop passes, genuine gate writes still blocked, end-to-end via headless Claude. Commit b941a19. | medium | — | pending |
| A11-ISS-3 | install.sh writes GitHub 404 body into .mcp.json — file is gitignored in source repo so download always fails; should skip instead of writing error body | ✅ Resolved 2026-07-23 — download_mcp_file() helper: curl -fsSL via temp file, dest untouched on failure, clear skip warning; .mcp.json now falls through to template as designed. Proven live (real 404 + bogus URL + success path). Commit 2d6a803. | low | — | pending |
## Recently closed

| ID | Title | Status | Commit | Detail |
|----|-------|--------|--------|--------|
| A11-ISS-2 | Two competing BOS-AI handoff conventions coexist: documents/foundations/+/foundations (current) vs ideation/+dev-setup (bos-ai-integration-guide, quickstart) — field-manual routes users away from /foundations | Resolved 2026-07-16 | 4adc939 | Legacy guides (bos-ai-integration-guide.md, bos-ai-quickstart.md) deleted; replaced by new canonical field-manual/bos-ai-handoff.md (two tiers + ownership-transfer rule, deployed via install.sh). README relinked (4 spots), root INTEGRATION-GUIDE.md slimmed to a pointer, 3 root design docs banner-marked as archive, example converted ideation/→documents/foundations/ with mapper-compatible filenames, dev-setup/dev-alignment missions given BOS-AI routing notes. PRJ-14 tasks 2–3. |
| A11-ISS-1 | install.sh omits foundation-*.schema.yaml (all 9) and validate_foundations.py — /foundations references dangling project/schemas/ paths in deployed projects | Resolved 2026-07-16 | 4adc939 | Schemas: all 9 added to install.sh schema_files, verified via test install (16 schemas land in ./schemas/). Dangling `project/schemas/` refs fixed → `schemas/` in foundations.md, plan.md, bootstrap.md, stack-profiles README. validate_foundations.py NOT deployed by decision: referenced nowhere, and stale (knows 5 of 9 categories) — stays internal. install.sh.sha256 regenerated. |
